import { type OverpassType } from "./type";

export const MODEL_TEMPERATURE = 0;

export const DEFAULT_POINT_TYPE: OverpassType = "node";

export const MIN_ROUTE_COUNT = 1;

export const MAX_ROUTE_COUNT = 3; // TODO extract these counts to config
